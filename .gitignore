# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear on external disks
.Spotlight-V100
.Trashes

# Claude Code local settings (but keep project config)
.claude/settings.local.json

# Root-level design drafts (frequently changed, not version-tracked)
/*.docx
